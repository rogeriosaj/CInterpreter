#include <stdio.h>

int main() {
    float a = 10.6;
    float b = 5.6;
    printf("A soma é: %d", a + b);
    printf("O produto é: %d", a * b);
    printf("A divisão de a por b é: %d", a / b);
    printf("A subtração de a por b é: %d", a - b);
    return 0;
}